<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title>فاکتور</title>
  <style>
    body {
      font-family: "Tahoma", Arial, sans-serif;
      direction: rtl;       /* Right-to-left layout */
      margin: 20mm;
      line-height: 1.5;
    }

    .page {
      width: 100%;
      max-width: 210mm;     /* A4 width */
      box-sizing: border-box;
    }

    h1 {
      text-align: center;
      color: #007bff;
      margin-bottom: 20px;
    }

    .invoice-header {
      margin-bottom: 20px;
    }

    .invoice-header p {
      margin: 2px 0;
      font-size: 14px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed;
      word-wrap: break-word;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 8px;
      text-align: right;       /* align right for RTL */
      word-break: break-word;
      font-size: 14px;
    }

    th {
      background-color: #f0f0f0;
    }

    /* Number styling to prevent overlap */
    .number {
      display: inline-block;  /* reserve space */
      min-width: 80px;        /* optional for alignment */
      text-align: left;       /* numbers LTR inside RTL context */
      direction: ltr;         /* force LTR for numerals */
    }

    .total {
      margin-top: 15px;
      font-weight: bold;
      text-align: right;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <div class="page">
    <h1>فاکتور</h1>

    <div class="invoice-header">
      <p><strong>مشتری:</strong> <%= customer %></p>
      <p><strong>تاریخ:</strong> <%= date %></p>
    </div>

    <table>
      <thead>
        <tr>
          <th>شرح کالا / خدمات</th>
          <th>قیمت (تومان)</th>
        </tr>
      </thead>
      <tbody>
        <% items.forEach(item => { %>
          <tr>
            <td><%= item.name %></td>
            <td><span class="number"><%= item.price.toLocaleString() %></span></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="total">
      جمع کل: <span class="number"><%= total.toLocaleString() %></span> تومان
    </div>
  </div>
</body>
</html>
